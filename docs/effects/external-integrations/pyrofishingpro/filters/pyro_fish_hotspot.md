# `pyro_fish_hotspot`
:::infoRequires:
PyroFishingPro
:::

If the fish was caught in a hotspot
# Filter Syntax
```yaml
filters:
  pyro_fish_hotspot: true # true = only activate in hotspots, false = only activate outside hotspots
```